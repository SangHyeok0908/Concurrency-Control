# Datasource is supplied by the Testcontainers MySQL container (@ServiceConnection).
# Override the main profile's validate so tests build the schema from entities.
spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
