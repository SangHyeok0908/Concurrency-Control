# 선착순 면접 예약 시스템 — 실험 계획

이 문서는 실험 순서와 범위를 다룬다. 프로젝트 배경과 최종 판단은 [README](README.md), 진행 상황과 산출물은 [브랜치 전략의 진행 표](docs/STEP2-3-BRANCH-STRATEGY.md)를 정본으로 한다.

## 방어 선택 원칙

가벼운 도구부터 도입하며, 각 단계에서 앞선 도구가 해결하지 못하는 문제를 확인한다.

1. **UNIQUE** — `(applicant_id, slot_id)` 제약으로 동일 요청의 중복 행을 막는다.
2. **조건부 UPDATE** — `remaining > 0` 검사와 차감을 한 SQL로 묶어 정원 경쟁을 다룬다.
3. **락** — 비관적 락과 낙관적 락을 같은 예약 부하에서 비교해 명시적 잠금과 충돌·재시도의 비용을 확인한다.

멱등성 키와 분산 락의 생략 결정 및 재검토 조건은 [브랜치 전략 ③](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key)과 [⑥](docs/STEP2-3-BRANCH-STRATEGY.md#skip-distributed-lock)에 둔다.

## 단계별 실험 설계

| 단계 | 할 일 | 확인할 근거 |
|---|---|---|
| 1. baseline | 락 없는 예약 로직에 동시 요청을 보낸다 | 실제 예약 수·잔여석·오버부킹·오류 기록 |
| 2. 방어 비교 | 방어를 하나씩 추가하고 정원 경쟁과 동일 요청을 분리해 측정한다 | DB 불변식·HTTP 응답·응답시간·처리량·재시도 비용 |
| 3. 판단 | 같은 조건의 결과를 비교하고 적용 경계를 정리한다 | 채택·생략 이유와 재검토가 필요한 도메인 변화 |

## 실험 통제 원칙

- 락 없는 baseline과 기존 재현 자료를 보존한다. 방어는 별도 전략으로 추가하고, 방어 하나당 브랜치·PR 하나로 이력을 남긴다.
- 각 전략을 같은 예약 시나리오에서 비교한다. 경합 조건과 환경 설정, 실행 순서, 반복 횟수를 기록한다. 상세 조건은 [벤치마크 문서](docs/STEP2-DEFENSE-BENCHMARK.md)를 따른다.
- 정합성과 HTTP 성공 여부를 먼저 확인한 뒤 성능을 해석한다. 빠르게 실패한 요청을 성공 처리량으로 읽지 않는다.
- 완료된 실험의 조건을 바꾸면 별도 범위로 측정하고 기존 결과와 섞지 않는다.

## 범위 밖

예약 취소·재예약, 다중 행 불변식, 외부 자원 연동은 현재 슬롯 한 행의 비교 실험과 분리한다. 예약 확정 뒤 알림·이메일 비동기화, CI/CD, 배포도 현재 범위 밖이다.

후속 기능이나 실험을 추가할 때는 baseline과 완료된 측정 자료를 보존하고 새 조건과 판단 근거를 별도로 남긴다.
