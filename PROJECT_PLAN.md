# 선착순 면접 예약 시스템 — 포트폴리오 프로젝트 기획서

> 이 문서는 Claude와의 대화를 통해 정리한 프로젝트 기획 및 개발 계획서입니다.
> 이후 대화에서 이 파일을 첨부하면 맥락을 이어서 작업할 수 있습니다.
>
> - **최초 작성일**: 2026-07-09
> - **문서 상태**: 1단계 완료, 2단계 진행 중
> - **진행 상황 정본**: [`docs/STEP2-3-BRANCH-STRATEGY.md`](docs/STEP2-3-BRANCH-STRATEGY.md)
>   — 이 기획서는 *무엇을 왜 하는가*의 정본이고, *어디까지 됐는가*는 그쪽 표를 본다.

---

## 1. 프로젝트 개요

### 배경 (Origin Story)
학부 시절 **동아리 지원자 관리 서비스**를 개발하며, 지원자들이 면접 일정을
**선착순으로 예약**하는 기능을 구현한 경험이 있다.

당시에는 동시성 문제(중복 예약, 정원 초과)를 **실제로 겪지는 않았지만**,
돌이켜보면 **구조적으로 언제든 터질 수 있는 코드**였다.
운이 좋았을 뿐, 검증된 적이 없는 상태였다.

이 프로젝트는 그때의 코드를 **"제대로 다시 만드는"** 리벤지 프로젝트다.
문제를 직접 재현하고, 여러 해결책을 비교 검증하며,
근거를 가지고 최종 방식을 선택하는 과정 전체를 기록하는 것이 목표다.

### 핵심 컨셉
> 단순 기능 구현이 아니라,
> **동시성 문제를 의도적으로 재현하고 → 여러 해결책을 비교 검증하는 실험형 프로젝트**

### 목표
- 신입 백엔드 개발자 취업용 포트폴리오
- "만들어봤다"가 아니라 **"문제를 분석하고 트레이드오프를 판단해 선택했다"**를 증명

### 기술 스택
| 구분 | 기술 |
|---|---|
| 언어 | **Java** |
| 프레임워크 | **Spring Boot** |
| ORM | Spring Data JPA |
| DB | MySQL |
| 캐시/분산락 | Redis (Redisson) |
| 부하 테스트 | Gatling |
| 배포 | AWS / Railway 등 (선택) |

---

## 2. 주제 평가 (왜 이 주제인가)

### 강점
- **실제 경험에서 나온 문제의식** → 지원동기·면접 스토리가 자연스럽다
- 동시성 제어는 실무 단골 주제 → 기술 면접(락, 트랜잭션 격리수준, 분산 환경)과 직결

### 위험 요소
- 티켓팅 / 선착순 쿠폰 / 좌석 예약 클론은 **국내 백엔드 신입 포트폴리오 단골 주제**
- 채용 담당자가 "또 이거네" 할 가능성 있음

### 결론
> 뻔함은 **주제의 문제가 아니라 깊이의 문제**다.
>
> - 그냥 "선착순 예약 CRUD" → ❌ 뻔함, 도움 안 됨
> - **문제 재현 → 분석 → 여러 해법 비교 → 선택 근거 서술** → ✅ 강한 포트폴리오

대부분의 지원자는 "선착순 버튼 만들고 `synchronized` 붙이면 끝" 수준에서 멈춘다.
이 프로젝트는 그 지점을 넘어서는 것이 차별화 포인트다.

---

## 3. 핵심 설계 판단: 무엇으로 방어할 것인가

### 문제를 두 종류로 분리해서 볼 것

가장 중요한 개념 정리. 이 둘은 **서로 다른 문제**이며, 해결책도 다르다.

| 문제 | 원인 | 해결책 |
|---|---|---|
| 같은 사용자가 요청을 두 번 보냄 (더블클릭, 네트워크 재시도) | **요청 자체의 중복** | 요청의 정체성을 고정하는 키 — 이 도메인에선 **자연 키 `UNIQUE(applicant_id, slot_id)`**, 자연 키가 없는 도메인이면 멱등성 키 |
| 다른 사용자들이 동시에 마지막 자리를 두고 경쟁 | **데이터 레이스 컨디션** | 락 / 조건부 UPDATE |

락(비관적/낙관적/분산) 3종만 구현하면 **경쟁 상황은 막아도 중복 요청은 못 막는다.**
실무에서는 이 두 문제가 항상 함께 온다.

> **갱신(2026-07-17).** 최초 기획은 위 왼쪽 칸의 해법으로 곧장 *멱등성 키*를 지목했지만, ①을 끝내고
> 다시 따져 보니 이 도메인에서는 **(지원자, 슬롯) 쌍이 그 자체로 요청의 정체성**이라 클라이언트가 별도
> 키를 발급할 이유가 없었다. 멱등성 키가 꼭 필요한 것은 결제·송금처럼 **요청 내용만으로 "같은 요청"인지
> 판별할 수 없는** 연산이다. 그래서 ③ `step2/idempotency-key`는 구현하지 않기로 했다 —
> 전체 근거는 [브랜치 전략 ③](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key).

### 왜 조건부 UPDATE + UNIQUE 제약이 "더 실무적"인가

단순 카운트 차감(정원 관리)에는 명시적 락(`SELECT ... FOR UPDATE`)보다
**원자적 조건부 UPDATE**를 먼저 고려하는 것이 실무 관행이다.

```sql
UPDATE interview_slot
SET remaining = remaining - 1
WHERE id = ? AND remaining > 0;
```

**비관적 락 대비 장점**
- 락 획득/해제 오버헤드 없음 → DB의 원자적 연산 하나로 끝
- 명시적으로 잡는 락이 없으니 **데드락 위험 없음**
- 단순 카운터 감소에는 사실상 최적해 → **처리량이 훨씬 높음**

여기에 `UNIQUE(user_id, slot_id)` 제약을 걸면
애플리케이션 로직에 버그가 있어도 **DB 레벨에서 중복 예약이 물리적으로 불가능**해진다.
애플리케이션 코드는 언제든 실수할 수 있지만, DB 제약은 거짓말을 하지 않는다.

### 그럼 락 3종은 왜 필요한가

**"언제 조건부 UPDATE만으로는 부족한가"를 보여주는 용도**로 배치한다.

- 조건부 UPDATE는 **단일 row의 단순 원자적 연산**일 때만 깔끔하게 통한다.
- "정원 확인 → 예약 기록 생성 → 알림 큐 적재"처럼
  **여러 단계 / 여러 테이블에 걸친 트랜잭션**이 필요하면 조건부 UPDATE 하나로는 안 된다.
  → 비관적 락 또는 분산 락이 필요해진다.
- 분산 락(Redis)은 **여러 서버 인스턴스 환경**에서
  애플리케이션 레벨 동기화가 필요할 때(예: 외부 API 호출이 포함된 임계 구역) 의미가 커진다.

### 방어 계층 최종 구성

> 핵심 서사: **"가장 단순하고 가벼운 해법부터 시도했고,
> 그것이 부족해지는 지점에서 더 무거운 도구(락)를 도입했다."**

| 순서 | 방어 수단 | 막는 문제 |
|---|---|---|
| 1 | **UNIQUE 제약** | 중복 예약 (DB 레벨 최후 방어선) — 자연 키가 멱등성 키 역할을 겸한다 |
| 2 | **조건부 UPDATE** | 정원 초과(오버부킹) |
| ~~3~~ | ~~**멱등성 키**~~ | **생략** — 1이 같은 문제를 이미 덮는다 ([근거](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key)) |
| 3 | **비관적 락 / 낙관적 락 / 분산 락** | 복잡한 다단계 트랜잭션에서의 경쟁 |

락부터 나열하는 것보다, 이 순서로 서술하는 편이
**엔지니어링 판단 과정**을 보여주기에 훨씬 유리하다.

---

## 4. 단계별 개발 계획

### 1단계: 기본 기능 + 문제 재현 (약 1주) — ✅ 완료

- [x] 도메인 설계: 지원자(Applicant), 면접 슬롯(InterviewSlot, 정원 N명), 예약(Reservation)
- [x] Spring Boot + Java + JPA + MySQL로 기본 CRUD 구현
- [x] 지원자 등록 / 면접 슬롯 조회 / 선착순 예약 API
- [x] **의도적으로 락 없이 구현**
- [x] Gatling으로 동시 요청 100~500개 부하 테스트
- [x] **정원 초과 / 중복 예약이 실제로 발생하는 것을 캡처**
      → [서비스 계층 재현](docs/STEP1-BASELINE-OVERBOOKING.md) · [HTTP 부하](docs/STEP1-GATLING-LOADTEST.md)

> 산출물: "그때는 몰랐던 문제를 이번엔 직접 눈으로 확인했다"는 증거

---

### 2단계: 방어 수단 구현 및 비교 (약 2주) — 진행 중

> 방어 하나 = 브랜치 하나 = PR 하나. 브랜치별 현재 상태는
> [`docs/STEP2-3-BRANCH-STRATEGY.md`](docs/STEP2-3-BRANCH-STRATEGY.md)의 진행 상황 표가 정본이다.

**2-1. 가벼운 해법부터**
- [x] `UNIQUE(user_id, slot_id)` 제약 추가 → 중복 예약 차단
      ([근거](docs/STEP2-UNIQUE-CONSTRAINT.md))
- [x] 조건부 UPDATE (`WHERE remaining > 0`) → 오버부킹 방지
      ([근거](docs/STEP2-CONDITIONAL-UPDATE.md))
- [x] ~~멱등성 키 도입~~ → **생략 결정 (2026-07-17)**
      자연 키 `UNIQUE(applicant_id, slot_id)`가 이미 같은 문제를 덮으므로 Redis `SETNX`를 얹지 않는다.
      "가벼운 것부터"라는 논지의 실천으로서의 생략 ([근거](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key))

**2-2. 락 방식 비교 구현**
- [ ] **비관적 락**: `@Lock(LockModeType.PESSIMISTIC_WRITE)` → `SELECT ... FOR UPDATE`
- [ ] **낙관적 락**: `@Version` 컬럼 + 재시도 로직 (지수 백오프)
- [ ] **분산 락**: Redis + Redisson `RLock`

**2-3. 벤치마크**
- [ ] 동일한 부하 시나리오로 각 방식 측정
- [ ] 측정 지표: **처리량(TPS), 평균/최대 응답시간, 실패율, 데이터 정합성**
- [ ] 결과를 표 + 그래프로 정리

---

### 3단계: 트레이드오프 분석 및 최종 선택 (3~4일)
- [ ] 각 방식의 장단점 및 적합한 상황 정리
- [ ] 이 프로젝트 규모(동아리 지원자 수십~수백 명)에 맞는 **현실적인 선택** 결정
- [ ] "트래픽이 훨씬 커진다면 어떻게 바꿀지" 확장 시나리오 서술
- [ ] 선택 근거를 README에 문서화

예시 결론 방향:
> 단일 서버 + 낮은 동시성 → 조건부 UPDATE + UNIQUE 제약으로 충분
> 다중 서버 인스턴스 → 분산 락 필요
> 복잡한 다단계 트랜잭션 → 비관적 락 검토

---

### 4단계: 마무리 (선택, 여유 있으면)
- [ ] 비동기 처리: 예약 확정 시 알림/이메일 발송을 큐로 분리
      (Spring `@Async` → Kafka/RabbitMQ로 확장)
- [ ] 테스트 코드 (동시성 통합 테스트 포함)
- [ ] CI/CD 간단 구성 (GitHub Actions)
- [ ] 배포 (AWS / Railway 등) + 접속 가능한 데모 링크

---

## 5. 포트폴리오 / README에 꼭 넣을 것

### 필수 포함 항목
1. **Before/After 부하 테스트 결과**
   - 방어 없을 때 vs 각 해결책 적용 시
   - 데이터 정합성 깨짐 여부 + 성능 수치 (TPS, 응답시간, 실패율)
   - 그래프/표로 시각화

2. **아키텍처 다이어그램**
   - 요청 흐름
   - **락이 걸리는 지점 / 방어 계층이 작동하는 지점** 명시

3. **트러블슈팅 회고**
   - 구체적인 삽질 기록이 핵심
   - 예: "낙관적 락 재시도 로직을 넣었더니 특정 상황에서 무한 재시도 발생
     → 재시도 횟수 제한 + 지수 백오프 추가"

4. **기술 선택의 근거**
   - 왜 처음부터 분산 락을 쓰지 않았는가
   - 왜 최종적으로 A가 아니라 B를 선택했는가
   - 다른 대안은 무엇이었고 왜 배제했는가

5. **프로젝트 개요 / 실행 방법**
   - 로컬 실행 가이드, 환경 변수, DB 스키마

### 코드 품질 체크리스트
- [ ] 의미 있는 단위의 커밋 + 명확한 커밋 메시지 (몰아서 커밋 1개 = 감점)
- [ ] 최소한의 단위 테스트 + 동시성 통합 테스트
- [ ] 코드 컨벤션 / 린트 적용
- [ ] 배포된 접속 가능한 링크

---

## 6. 예상 면접 질문 (미리 준비)

### 동시성 기본
- 비관적 락과 낙관적 락의 차이는? 각각 언제 쓰는가?
- MySQL 격리 수준(READ COMMITTED / REPEATABLE READ)과 락의 관계는?
- `SELECT ... FOR UPDATE`는 어떤 락을 잡는가? 갭 락(Gap Lock)은 언제 발생하는가?

### 설계 판단
- 왜 처음부터 분산 락을 쓰지 않고 조건부 UPDATE부터 시도했는가?
- 조건부 UPDATE만으로 부족해지는 경우는 언제인가?
- UNIQUE 제약이 있는데도 애플리케이션 레벨 방어가 필요한 이유는?

### 멱등성 (구현하지 않기로 판단한 항목 — [근거](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key))
- 멱등성 키는 어떤 문제를 해결하는가? 락과 무엇이 다른가?
- **왜 이 프로젝트는 멱등성 키를 도입하지 않았는가?** (자연 키가 요청의 정체성을 이미 고정한다)
- 어떤 조건이 바뀌면 멱등성 키가 필요해지는가? (예약에 결제가 붙거나, 같은 슬롯 복수 예약이 허용되면)
- 그때 TTL은 어떻게 정하겠는가? 첫 요청 처리 중에 같은 키로 두 번째 요청이 오면?
- UNIQUE 제약만으로 남는 빈틈은 무엇인가? (재시도한 클라이언트가 `200 + 기존 예약`이 아니라 `409`를 받는다)

### 분산 락
- Redis 분산 락에서 락 획득 후 서버가 죽으면 어떻게 되는가? (TTL 설계)
- 락 TTL이 만료됐는데 원래 작업이 아직 안 끝났다면? (Redisson watchdog)
- Redlock 알고리즘의 한계는?

### 확장성
- 트래픽이 100배 늘어난다면 어떤 부분을 먼저 바꾸겠는가?
- 서버 인스턴스가 여러 대가 되면 무엇이 깨지는가?

---

## 7. 다음 할 일 (Next Actions)

다음에 무슨 브랜치를 파는지는 [`docs/STEP2-3-BRANCH-STRATEGY.md`](docs/STEP2-3-BRANCH-STRATEGY.md)의
진행 상황 표를 본다 — 여기 목록을 따로 두면 반드시 어긋나므로 중복해 적지 않는다.

기획 단계에서 남은 것(브랜치에 속하지 않는 항목)만 여기 둔다.

- [x] ERD 설계 (Applicant / InterviewSlot / Reservation) → `docs/ERD.md`
      (IdempotencyKey 테이블도 설계했으나 미채택 — ERD 2.4에 기록만 남김)
- [ ] 시퀀스 다이어그램 (예약 요청 흐름 + 방어 계층) — README 아키텍처 다이어그램 섹션과 함께 작성

---

## 부록: 일반 포트폴리오 원칙 (초기 대화 요약)

### 주제 선정
- 본인이 겪은 불편함에서 출발 → 지원동기가 자연스럽고 애정이 드러남
- 2주~2개월 내 완성 가능한 스코프 → 미완성 대형 > 완성된 소형 (X)
- 기술 스택 선택 이유를 설명할 수 있어야 면접에서 방어 가능

### 기술적 깊이
- CRUD를 넘어서는 요소 1개 이상 (인증, 실시간, 캐싱, 동시성, 배치 등)
- 트레이드오프를 의식적으로 선택하고 기록
- 예외 처리 / 엣지 케이스 / 부하 상황 고려

### 스토리텔링
- "무엇을 만들었다"보다 **"어떤 문제를 어떻게 해결했다"**
- 트러블슈팅 경험 1~2개는 반드시 정리 (면접 최다 질문)

### 양보다 질
- 여러 개의 어설픈 프로젝트 < **1~2개를 깊이 있게 완성**
