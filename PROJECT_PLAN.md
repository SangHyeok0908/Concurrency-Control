# 선착순 면접 예약 시스템 — 프로젝트 기획

> 이 문서는 **무엇을 왜 실험하는가**를 설명한다. 브랜치별 진행 현황과 세부 결정의 정본은 [`docs/STEP2-3-BRANCH-STRATEGY.md`](docs/STEP2-3-BRANCH-STRATEGY.md)다.

## 프로젝트 목적

학부 동아리 지원자 관리 서비스에서 만들었던 선착순 면접 예약 기능을 다시 검증한다. 당시에는 문제가 드러나지 않았지만, 동시 요청 아래에서 정원 초과와 중복 예약이 생길 수 있는 구조였다.

목표는 예약 CRUD를 만드는 데 있지 않다. 락 없는 baseline에서 실패를 재현하고, 방어 수단을 하나씩 같은 조건으로 비교한 뒤, 최종 선택과 생략한 선택의 근거를 남기는 동시성 제어 실험이다. 포트폴리오에서 증명하려는 것은 “구현했다”가 아니라 “문제 모양과 측정 결과로 트레이드오프를 판단했다”는 점이다.

## 문제를 두 종류로 분리한다

| 문제 | 원인 | 이 프로젝트의 방어 |
|---|---|---|
| 같은 사용자의 중복 요청 | 더블클릭·재시도 | 자연 키 `UNIQUE(applicant_id, slot_id)` |
| 다른 사용자의 정원 경쟁 | read-check-write 레이스 | 조건부 UPDATE 또는 락 |

락은 후자를 해결해도 전자를 해결하지 못한다. 반대로 UNIQUE는 중복 요청을 막아도 서로 다른 지원자의 마지막 자리 경쟁을 막지 못한다. 이 분리가 방어 수단을 비교하는 기준이다.

## 방어 선택 원칙

가벼운 도구부터 도입한다. 먼저 `UNIQUE(applicant_id, slot_id)`로 DB 수준 중복을 막고, `UPDATE ... WHERE remaining > 0`으로 정원 확인과 감소를 원자적으로 묶는다. 비관적·낙관적 락은 다중 행 불변식이나 여러 단계의 트랜잭션처럼 한 조건부 DML로 접을 수 없을 때 비교할 도구다.

두 선택은 의도적으로 생략했다.

- **멱등성 키**: 이 도메인에서는 `(applicant, slot)` 자연 키가 요청의 정체성을 이미 고정하고 UNIQUE가 이를 강제한다. 결제처럼 같은 내용의 요청이 서로 다른 연산일 수 있거나 동일 슬롯 복수 예약을 허용할 때 다시 검토한다. [판단 근거](docs/STEP2-3-BRANCH-STRATEGY.md#skip-idempotency-key)
- **분산 락**: 임계 구역은 단일 MySQL 슬롯 행의 UPDATE로 끝난다. 애플리케이션 인스턴스를 늘리는 것만으로는 분산 락의 근거가 되지 않는다. 외부 결제·다른 저장소·정확히 한 번 처리할 알림처럼 DB 밖 자원을 함께 조율할 때 다시 검토한다. [판단 근거](docs/STEP2-3-BRANCH-STRATEGY.md#skip-distributed-lock)

조건부 UPDATE도 InnoDB 행 잠금을 사용하며 예약 INSERT와 트랜잭션 커밋까지 유지한다. 주변 SQL과 결합하면 데드락이 가능하다. v2 200회에서 KO 0은 해당 시나리오의 관측값이다.

## 단계별 결과 요약

| 단계 | 결과 | 근거 |
|---|---|---|
| 1. baseline 재현 | 락 없는 경로에서 오버부킹·lost update·데드락을 재현 | [서비스 계층](docs/STEP1-BASELINE-OVERBOOKING.md) · [HTTP 부하](docs/STEP1-GATLING-LOADTEST.md) |
| 2. 방어 구현·비교 | UNIQUE, 조건부 UPDATE, 비관적 락, 낙관적 락을 구현하고 정원 경쟁 v2 200회·동일 요청 25회 측정 | [벤치마크](docs/STEP2-DEFENSE-BENCHMARK.md) |
| 3. 최종 선택 | `UNIQUE` + 조건부 UPDATE를 선택. 성능 순위가 아니라 현재 불변식을 가장 작게 표현한다는 이유 | [README 분석](README.md#트레이드오프와-최종-선택) |

방어 하나 = 브랜치 하나 = PR 하나라는 이력도 실험의 일부다. 상태와 세부 산출물은 [브랜치 전략의 진행 표](docs/STEP2-3-BRANCH-STRATEGY.md)를 따른다.

## 최종 판단

`UNIQUE`와 조건부 UPDATE는 서로 다른 불변식을 맡는 조합이다. 동일 요청은 DB 제약으로 막고, 정원 경쟁은 한 조건부 DML로 해결한다. 비관적 락은 통제 측정에서 극단 경합에 유리했지만, 현재 도메인에는 명시적 read→check→decrease 잠금 구간이 필요하지 않아 선택하지 않았다. 낙관적 락은 측정한 상한에서 재시도 비용 또는 가용성 손실이 컸다.

상세 수치, 아키텍처, 확장 경계와 후속 API 과제는 [README](README.md)에, 개별 방어의 구현 근거는 `docs/STEP2-*.md`에 둔다.

## 4단계 범위 밖

예약 확정 뒤의 알림·이메일 비동기화, CI/CD, 배포는 현재 비교 실험의 범위 밖이다. 추후 추가하더라도 baseline과 완료된 1~3단계의 측정 조건을 바꾸지 않고 별도 범위로 다룬다.
